import React from 'react';

import AccountsPage from '@features/accounts/pages';

const page: React.FC = () => {
    return (
        <>
            <AccountsPage />
        </>
    );
};

export default page;
