import AccountsPage from '@/modules/accounts';
import React from 'react';

const page: React.FC = () => {
    return (
        <>
            <AccountsPage />
        </>
    );
};

export default page;
