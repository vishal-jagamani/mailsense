import React from 'react';

import AccountsPage from '@modules/accounts';

const page: React.FC = () => {
    return (
        <>
            <AccountsPage />
        </>
    );
};

export default page;
