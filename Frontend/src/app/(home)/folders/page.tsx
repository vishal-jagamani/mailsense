import FoldersPage from '@/modules/folders';
import React from 'react';

const page: React.FC = () => {
    return (
        <>
            <FoldersPage />
        </>
    );
};

export default page;
